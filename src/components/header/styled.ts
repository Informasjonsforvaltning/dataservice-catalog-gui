import styled from 'styled-components';

const Header = styled.div`
  & > header {
    padding: 3rem 0;

    & > div > button > span {
      font-size: 1rem;
    }
  }
`;

export default {
  Header
};
